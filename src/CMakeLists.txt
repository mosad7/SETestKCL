
set(SETestKCL_INCLUDE_DIRS "")

set(CORE_CXX "")
set(CORE_H "")
ADD_SUBDIRECTORY(Core)

INCLUDE_DIRECTORIES(${SETestKCL_INCLUDE_DIRS})

ADD_SUBDIRECTORY(TextualUI)

if(${Build_GUI_App})
ADD_SUBDIRECTORY(GUI)
endif()